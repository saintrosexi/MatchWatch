# База данных MatchWatch

Схема Postgres, RLS-политики и вся доменная логика комнат.

## Как устроено

| Файл | Что внутри |
|---|---|
| `20260825090000_core_identity_and_taste.sql` | Профиль, идентичности, профиль вкуса, история реакций, избранное, мэтчи |
| `20260825090100_rooms_and_catalog.sql` | Комнаты, участники, голоса, мэтчи, список к просмотру, кэш каталога TMDB, рантайм-конфиг |
| `20260825090200_telemetry_and_analytics.sql` | Журнал событий, продуктовые метрики, когорты, представление и функции дашборда |
| `20260825090300_room_rpc_functions.sql` | Операции с комнатами: создание, вход, голос+мэтч, присутствие, TTL-уборка |
| `20260825090400_row_level_security.sql` | RLS на каждую таблицу, публикация Realtime |
| `20260825090500_harden_function_grants.sql` | Сужение прав на вызов функций, `security_invoker` для представления |

## Применение

```bash
supabase link --project-ref dthzextbetnwjnepbdad
supabase db push
```

Миграции уже применены к облачному проекту — эти файлы держат схему
в version control, чтобы её можно было воспроизвести с нуля и увидеть
историю изменений в ревью.

Стянуть текущее состояние обратно:

```bash
supabase db pull
```

## Ключевые решения

**Логика комнат живёт в SQL, а не в клиенте.** `record_swipe` берёт
блокировку на строку комнаты и в одной транзакции записывает голос,
считает кворум и создаёт мэтч. Иначе два последних голоса, поданных
одновременно, под READ COMMITTED не видят друг друга, и мэтч не рождается.

**RLS — единственная настоящая граница.** Anon-ключ публичный по
определению: он лежит в бандле клиента. Всё, что не разрешено политикой,
недоступно.

**Уровень доступа пишет только сервер.** RLS не разграничивает по
колонкам, поэтому `authenticated` получает `UPDATE` точечно на
`display_name`, `photo_url` и `locale` — `access_tier` и `access_stars`
клиенту недоступны.


## Уборка комнат и тариф Vercel

Крон `/api/ops/rooms-gc` запускается раз в сутки, а не ежечасно: на тарифе
Hobby чаще нельзя. На корректность это не влияет — `join_room` сам
отказывает по истёкшему `expires_at`, поэтому в мёртвую комнату не попасть
независимо от того, когда её удалят. Отличается только то, как долго
мусорные строки занимают место и коды: до суток вместо часа. При 923 тысячах
возможных кодов это несущественно; на платном тарифе расписание можно
вернуть к `0 * * * *`.
