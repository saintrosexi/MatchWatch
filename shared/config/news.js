/**
 * MatchWatch — что нового.
 *
 * Один список на две поверхности: баннер, который показывается один раз
 * при следующем заходе, и страница релизов. Держать их раздельно нельзя —
 * тогда баннер обещал бы одно, а страница рассказывала бы другое.
 *
 * ────────────────────────────────────────────────────────────────
 * ГДЕ ЧТО ПРАВИТЬ
 *
 *   Текст объявления и новостей ......... этот файл, массив `NEWS`
 *   Цена в звёздах и рублях ............. shared/config/premium.js,
 *                                         поле `price`
 *   Список возможностей подписки ........ shared/config/premium.js,
 *                                         поле `benefits` (он же
 *                                         подставляется сюда по `listFrom`)
 *
 * Больше нигде эти строки не дублируются: витрина, объявление
 * и страница обновлений читают их отсюда.
 * ────────────────────────────────────────────────────────────────
 * КАК ДОБАВИТЬ ЗАПИСЬ
 *
 * Дописать объект В НАЧАЛО массива. Порядок в файле и есть порядок
 * на странице: сортировки по дате нет намеренно, иначе опечатка
 * в дате молча перемешала бы историю продукта.
 *
 * `id` менять нельзя никогда. По нему запоминается, что человек уже
 * видел эту новость; смена идентификатора покажет её всем повторно.
 * ────────────────────────────────────────────────────────────────
 */

/** Метки. Цвет и слово, больше ничего — категорий много не нужно. */
export const NEWS_TAG = Object.freeze({
  PREMIUM: 'premium',
  FEATURE: 'feature',
  FIX: 'fix',
});

export const NEWS_TAG_LABEL = Object.freeze({
  [NEWS_TAG.PREMIUM]: 'Премиум',
  [NEWS_TAG.FEATURE]: 'Новое',
  [NEWS_TAG.FIX]: 'Исправили',
});

/**
 * Записи. `body` — до списка возможностей, `tail` — после него.
 * Разделение нужно только записям с `listFrom`; у остальных `tail` нет.
 */
export const NEWS = [
  {
    id: '2026-08-31-premium',
    date: '2026-08-31',
    tag: NEWS_TAG.PREMIUM,
    title: 'MatchWatch Premium',
    /** Строка для баннера. Одна, короткая, без подробностей. */
    lead: 'Узнайте о новых возможностях — на первый месяц бесплатно.',
    /**
     * Показывать ли эту запись объявлением во весь экран при следующем
     * заходе.
     *
     * ТОЛЬКО У ОДНОЙ ЗАПИСИ ЗА РАЗ. Два полноэкранных объявления подряд
     * человек не читает — он закрывает оба. Выпуская новое обновление,
     * снимите флаг у прежней записи и поставьте у новой.
     *
     * Всё, у чего флага нет, живёт в «Что нового» и никого не перебивает.
     */
    banner: true,
    /** Баннер ведёт прямо в витрину, а не на страницу новостей. */
    action: 'premium',
    body: [
      'У MatchWatch появилась подписка. Коротко: она снимает границы '
      + 'бесплатного тарифа и открывает оформление профиля.',
      'Без подписки всё главное остаётся на месте — лента, свайпы, списки, '
      + 'оценки и совместный выбор кино. Ограничений три: три комнаты '
      + 'в месяц, шесть фильмов в визитке и недоступный подбор по фразе.',
    ],

    /**
     * Список возможностей берётся из конфига подписки, а не пишется
     * здесь заново. Две копии одного перечня разъезжаются на первой же
     * правке тарифа, и тогда страница обещает не то, что даёт витрина.
     */
    listFrom: 'premium',

    tail: [
      'Про комнаты подробнее: три в месяц считаются скользящим окном '
      + 'за тридцать дней, а не сбрасываются первого числа. Вступать '
      + 'в чужие комнаты можно сколько угодно — ограничение только '
      + 'на создание своих.',
      'Подбор по фразе — это когда вместо фильтров пишешь «хочу сегодня '
      + 'что-то мрачное и не длинное», и лента понимает. Он в подписке '
      + 'потому, что каждый такой разбор стоит нам денег у модели.',
      'На время закрытого теста подписка стоит ноль рублей вместо 99. '
      + 'Карту привязывать не нужно, ничего не спишется, и продлеваться '
      + 'сама она не будет — когда месяц кончится, мы просто спросим ещё раз. '
      + 'Дальше 99 ₽ или 75 звёзд в месяц.',
    ],
  },
  {
    id: '2026-08-30-rating-prompt',
    date: '2026-08-30',
    tag: NEWS_TAG.FEATURE,
    title: 'Оценка сразу после просмотра',
    lead: 'Отметили фильм просмотренным — спросим, как он вам.',
    banner: false,
    body: [
      'Свайп вверх отмечает фильм просмотренным, и теперь у карточки '
      + 'появляется просьба поставить оценку — десять звёзд в одно касание.',
      'Оценка решает в подборке больше свайпа: выше шести — лента несёт '
      + 'похожее, ниже — уводит от него. Свайп такого не умеет, он всегда «да».',
      'Если просьба мешает, её можно выключить насовсем — в настройках '
      + 'профиля она включается обратно.',
    ],
  },
  {
    id: '2026-08-30-feed-modes',
    date: '2026-08-30',
    tag: NEWS_TAG.FEATURE,
    title: 'Две ленты вместо одной',
    lead: '«Моё» — спокойный вечер, «Другое» — поиск незнакомого.',
    banner: false,
    body: [
      'Человек приходит в приложение в разном состоянии, и одна лента '
      + 'для «просто полистать своё» и для «хочу чего-то другого» одинаково плоха.',
      '«Моё» — предсказуемая подборка: в основном то, что уже нравится. '
      + '«Другое» начинается с громкого незнакомого и разворачивается за тем, '
      + 'что вы лайкнули минуту назад.',
      'Выбор живёт только в этой сессии: перезашли — лента снова обычная. '
      + 'Постоянный вкус от переключения не меняется.',
    ],
  },
];

/** Запись для баннера — первая помеченная. Ноль записей это тоже норма. */
export const bannerNews = () => NEWS.find((item) => item.banner) ?? null;

/** Все идентификаторы: ими помечается «прочитано». */
export const newsIds = () => NEWS.map((item) => item.id);
