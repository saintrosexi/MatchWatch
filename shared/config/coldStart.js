/**
 * Стартовый набор — калибровка вкуса.
 *
 * До первых свайпов профиль пуст, и порядок ленты определяется одним лишь
 * качеством фильма: у всех получается одно и то же начало. Набор решает
 * не эту косметику, а задачу под ней — за полтора десятка карточек понять,
 * что человеку вообще нравится.
 *
 * Поэтому отбор идёт по НЕПОХОЖЕСТИ, а не по любимости. Пятнадцать
 * блокбастеров одного жанра дадут пятнадцать ответов об одной грани вкуса;
 * рядом стоящие космический эпос и советская комедия — о пятнадцати
 * разных. Каждая строка закрывает свою ось, и дубликатов осей нет.
 *
 * Состав держится на трёх опорах:
 *   — половина свежего, половина проверенного временем: аудитория разного
 *     возраста должна узнать хотя бы часть списка;
 *   — три отечественных фильма: отношение к своему кино — сильный сигнал,
 *     которого мировая классика не даёт;
 *   — только полный метр. Длинный формат — отдельная грань вкуса, но
 *     смешивать её с калибровкой по фильмам значит спрашивать о двух
 *     вещах одним вопросом.
 *
 * Список правится руками: это редакторское решение, а не выборка.
 * Пересматривать стоит раз в год — состав «того, что видели все» меняется
 * медленно, но меняется.
 */

export const COLD_START_TITLES = Object.freeze([
  { id: 438631, note: 'космос, политический эпос, медленный размах' },
  { id: 680, note: 'криминал, диалоги, нелинейная структура' },
  { id: 120, note: 'фэнтези, поход, героический эпос' },
  { id: 129, note: 'аниме, сказка, совсем другой темп' },
  { id: 694, note: 'классический хоррор, нарастающий ужас' },
  { id: 11036, note: 'мелодрама в чистом виде' },
  { id: 245891, note: 'чистый экшен без подтекста' },
  { id: 545611, note: 'абсурд, жанровый винегрет, эмоция' },
  { id: 346698, note: 'комедия-сатира, яркая картинка' },
  { id: 872585, note: 'биография, разговорная драма, история' },
  { id: 933260, note: 'боди-хоррор, провокация, чёрная сатира' },
  { id: 1064213, note: 'независимая драма, живая, без глянца' },
  { id: 20992, note: 'отечественный криминал девяностых, культ' },
  { id: 20803, note: 'советская комедия, ностальгия' },
  { id: 751171, note: 'семейное кино, детское, доброе' },
]);

export const COLD_START_IDS = Object.freeze(COLD_START_TITLES.map((t) => t.id));

/** Быстрая проверка принадлежности: вызывается на каждый тайтл при ранжировании. */
const ID_SET = new Set(COLD_START_IDS);

export const isColdStartTitle = (title) =>
  Boolean(title) && ID_SET.has(Number(title.externalId ?? title.id));
